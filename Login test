//logged in using the username and password
//Simple login scenario

package SampleTest;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;  

public class FirstTest {
	 
	public static void main(String[] args) throws InterruptedException {
       
		WebDriver driver = new ChromeDriver();
		
		driver.manage().window().maximize();

        driver.get("https://practicetestautomation.com/practice-test-login/");
        Thread.sleep(3000);
        
        WebElement username = driver.findElement(By.id("username"));
        username.sendKeys("student");
        WebElement password = driver.findElement(By.id("password"));
        password.sendKeys("Password123");
        WebElement submit = driver.findElement(By.id("submit"));
        submit.click();

        Thread.sleep(2000);
        driver.quit();
    }
}

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//login and logout scenarios:
//also used explicit waits.

package SampleTest;

import java.time.Duration;
import java.util.Arrays;
import java.util.HashSet;

import org.openqa.selenium.Alert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

public class Main {
	public static void main(String[] args) throws InterruptedException {
		WebDriver driver = new ChromeDriver();
		driver.manage().window().maximize();
//url		
		String exp_url = "https://practicetestautomation.com/practice-test-login/";
		driver.get(exp_url);
//explicit wait		
		WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(5));
        wait.until(ExpectedConditions.visibilityOfElementLocated(By.id("submit")));
//webelements        
		WebElement username = driver.findElement(By.xpath("//input[@id=\"username\"]"));
		username.sendKeys("student");
		WebElement password = driver.findElement(By.id("password"));
		password.sendKeys("Password123");
		WebElement submit = driver.findElement(By.cssSelector(".btn"));
		submit.click();
		String exp_title = "Logged In Successfully | Practice Test Automation";
		String post_title = driver.getTitle();
//Validations		
		if(exp_title.equals(post_title)) {
			System.out.println("Logged in successfull");
		}
		else {
			System.out.println("Something wrong....Try again");
		}
//Validation 2		
		WebElement value = driver.findElement(By.className("post-title"));
		String actualTitle = value.getText();
		Thread.sleep(3000);
		if(actualTitle.equals("Logged In Successfully")) {
			System.out.println("Expected matches");
		}
//logout		
		WebElement logout = driver.findElement(By.xpath("//a[text()='Log out']"));
		logout.click();
		
		System.out.println("Logged out successfully..");
//closing		
		driver.close();
		
		System.out.println("Chrome was closed");
	}
}

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//Negative test scenario
//Invalid username / password

package SampleTest;

import java.time.Duration;
import java.util.Arrays;
import java.util.HashSet;

import org.openqa.selenium.Alert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

public class Main {
	public static void main(String[] args) throws InterruptedException {
		WebDriver driver = new ChromeDriver();
		driver.manage().window().maximize();
		
		String exp_url = "https://practicetestautomation.com/practice-test-login/";
		driver.get(exp_url);
		
		WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(1));
        wait.until(ExpectedConditions.visibilityOfElementLocated(By.id("submit")));
        
        System.out.println("Chromer opened.");
        
		WebElement username = driver.findElement(By.xpath("//input[@id=\"username\"]"));
		username.sendKeys("username");
		WebElement password = driver.findElement(By.id("password"));
		password.sendKeys("Password123");
		WebElement submit = driver.findElement(By.cssSelector(".btn"));
		submit.click();
		
		wait.until(ExpectedConditions.visibilityOfElementLocated(By.className("show")));
		WebElement value = driver.findElement(By.className("show"));
		String expected_text = value.getText();
		if(expected_text.equals("Your username is invalid!")) {
			System.out.println("Username Text matched");
		}
// this block will not be executed as i intendly entered the username as invalid -> entered the password correctly.
		wait.until(ExpectedConditions.visibilityOfElementLocated(By.id("error")));
		WebElement value1 = driver.findElement(By.id("error"));
		String expected_text1 = value1.getText();
		if(expected_text1.equals("Your password is invalid!")) {
			System.out.println("Password Text matched");
		}
		
		Thread.sleep(2000);
		
		System.out.println("Chrome closed");
		driver.quit();
	}
}

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

